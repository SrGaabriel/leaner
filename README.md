# 🪶 leaner

A linter, formatter and whole-program dead code eliminator for Lean 4.

# Installation

To install leaner, run the following commands in your terminal:

```bash
git clone https://github.com/SrGaabriel/leaner.git
cd leaner
lake build
lake run install
```